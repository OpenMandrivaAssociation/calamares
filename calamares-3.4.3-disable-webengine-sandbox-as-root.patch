diff -up calamares-3.4.3/src/calamares/main.cpp.omv~ calamares-3.4.3/src/calamares/main.cpp
--- calamares-3.4.3/src/calamares/main.cpp.omv~	2026-09-09 00:00:00.000000000 +0200
+++ calamares-3.4.3/src/calamares/main.cpp	2026-09-14 03:23:16.000000000 +0200
@@ -28,6 +28,7 @@
 #include <QDir>
 
 #include <memory>
+#include <unistd.h>
 
 /** @brief Gets debug-level from -D command-line-option
  *
@@ -110,6 +111,13 @@ main( int argc, char* argv[] )
     // Not needed in Qt6
     QApplication::setAttribute( Qt::AA_EnableHighDpiScaling );
 #endif
+    // QtWebEngine/Chromium refuses to start as uid 0 unless the sandbox is
+    // disabled. Calamares needs root for partitioning; set this before
+    // QApplication so a CLI launch as root still gets a working WebEngine.
+    if ( getuid() == 0 || geteuid() == 0 )
+    {
+        qputenv( "QTWEBENGINE_DISABLE_SANDBOX", "1" );
+    }
     CalamaresApplication a( argc, argv );
 
     KAboutData aboutData( "calamares",
